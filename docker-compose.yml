version: '3.8'

services:
  api:
    build: .
    container_name: ai_orchestrator_api
    ports:
      - "3000:3000"
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: redis:7
    container_name: ai_orchestrator_redis
    ports:
      - "6379:6379"