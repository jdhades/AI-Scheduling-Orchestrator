# Env de tests de integración. Solo cargado cuando `jest.config.js` corre
# el project "integration" — los unit tests no lo tocan.
#
# Valores apuntan al stack local de Supabase (`pnpm dlx supabase start`).
# Las claves de servicio son las que el CLI imprime en `supabase status`
# — locales y públicas por design (no son secretos productivos).

SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=REDACTED_SUPABASE_SECRET
SUPABASE_ANON_KEY=sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH

# postgres expuesto en 54322 por el CLI.
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres
