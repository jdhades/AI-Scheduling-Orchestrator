select employees.name as name, shifts.start_time as inicio, shifts.end_time as fin from shifts  inner join shift_assignments  on shifts.company_id = shift_assignments.company_id and shifts.id =shift_assignments.shift_id inner join employees on shift_assignments.employee_id = employees.id where EXTRACT(DAY FROM shifts.start_time) > 12 order by shifts.start_time 